import React from 'react';
import './footer.scss';
import { motion } from 'framer-motion';

const Footer = () => {
  return (
    <section className="h-[40vh] .footer__bg">
      <div>
        <motion.h1
          intial={{}}
          animate={{}}
          whileInView={{}}
          className="">The time to invest in the </motion.h1>
      </div>
    </section>
  );
};

export default Footer;
